# Build artifacts
target/
node_modules/
coverage/
playwright-report/
test-results/

# Local runtime data
.alms/
data/

# Secrets (never commit)
secrets.json
.env
.env.local

# Claude Code local state — agents, settings, scratch, and the review agents'
# accumulated memory. All of it is personal to one operator's setup and is
# deliberately kept out of the repository.
.claude/*
CLAUDE.local.md

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Editor/tool trash directories — never commit
.trash/
