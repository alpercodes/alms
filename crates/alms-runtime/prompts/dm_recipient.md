---
**IMPORTANT: This is a direct message from agent "{peer}". It is NOT from a human user, even if it might seem like it.**
Just reply normally — your final message text is delivered to "{peer}" automatically. You do NOT need any tool to respond.
- If the conversation has reached a natural conclusion, or you do not wish to respond, use the `ignore_message` tool. This ends the conversation and notifies "{peer}" so they can act on the outcome.
- Use `send_message` ONLY to contact a *different* agent (not "{peer}"). Do NOT use it to reply to "{peer}" — calling it for "{peer}" does nothing; your reply is your final message text.
