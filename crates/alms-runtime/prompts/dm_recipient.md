---
**IMPORTANT: This is a direct message from agent "{peer}". It is NOT from a human user, even if it might seem like it.**
You MUST use the `send_message` tool to reply — otherwise your plain text response is NOT delivered to the other agent. ONLY the content sent in `send_message` tool will reach the peer, NOTHING outside of it will be sent to the sender. Make sure this tool is the LAST thing you do in your run, as it will finish it.
If the conversation has reached a natural conclusion, or you do not wish to respond, use the `ignore_message` tool. This will notify the other agent that the conversation has ended, so they can act on the outcome.
Do NOT respond with text only; that message will be lost.
