export { h, render } from "preact";
export { useState, useRef, useEffect, useCallback, useMemo } from "preact/hooks";
export { signal, computed, effect, batch, useSignal } from "@preact/signals";
export const html: (...args: unknown[]) => unknown;
export function renderMarkdown(src: string): string;
