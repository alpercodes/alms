# ALMS — Claude Code Instructions

## What is this project?

ALMS (Agent Loop Management System) is a Rust-based multi-agent coordination platform. A single daemon exposes an HTTP/SSE API, runs agent loops against LLM providers, executes native tools with per-tool sandboxing (project-root path canonicalization, shell permissions, Landlock on Linux — see [`docs/security-model.md` § 4.4](docs/security-model.md#filesystem-sandboxing) for the platform asymmetry between Linux and Windows/macOS), and manages sessions with snapshot persistence.

## Build & Run

```bash
# Requires Rust nightly (auto-installed from rust-toolchain.toml)
# On Windows, cargo is at ~/.cargo/bin/cargo — use `export PATH="$HOME/.cargo/bin:$PATH"` if needed
cargo build --release
cargo run --bin alms -- gateway --bind 127.0.0.1:8080

# Run all CI checks locally
make ci          # fmt-check + clippy + test + build-release
make test        # cargo test --all
make test-golden # SSE golden tests only
make clippy      # cargo clippy -- -D warnings
```

## Project Structure

```
crates/
  alms-core/         # Core types (IDs, Capability, AuditEvent, Channel trait, errors)
                     #   config.rs — unified AlmsConfig (layered: defaults → TOML → env vars)
                     #   registry.rs — AgentRecord, CreateAgentRequest, validate_agent_name
  alms-gateway/      # Axum HTTP server, SSE streaming, run lifecycle, event log
  alms-runtime/      # Agent loop, LLM client, tool registry, context builder, workspace
                     #   agent/mod.rs — AgentRuntime struct, public API, run orchestration
                     #   agent/loop_impl.rs — agent_loop(), stream_llm_call(), execute_tool_call()
                     #   agent/context.rs — build_context(), episodic summaries, summarization
                     #   agent/dm.rs — DM-specific helpers, conflict detection
                     #   agent/types.rs — Posture, AgentConfig, SystemPrompts, RunOutput
                     #   context/mod.rs — ContextBuilder orchestrator (token-budgeted context window)
                     #   context/normalize.rs — canonical message-shape pipeline (group/strip/normalize)
                     #   context/strategies.rs — token-budgeted history selection (full/truncate/sliding)
                     #   context/perspective.rs — DM-perspective role mapping + reasoning-message filter
                     #   context/rebuild.rs — persisted Message → LlmMessage reconstruction (incl. spill repair)
                     #   context/error_markers.rs — error-marker classification + legacy run-boundary dedup
                     #   workspace.rs — AgentWorkspace (personality/goals/memories/user files)
                     #   episodic.rs — Cross-session episodic memory (summary generation + formatting)
                     #   workspace_tool.rs — WorkspaceWriteTool (exception: stays in runtime)
  alms-tools/        # Tool implementations extracted from alms-runtime
                     #   8 agent tools (send_message, invoke_agent, read_session, etc.)
                     #   SubagentDispatcher, MessageSender traits
                     #   EventForwarder trait for type-erased runtime event forwarding
  alms-coordinator/  # Multi-agent orchestration — pure hierarchy, real AgentRuntime loops
  alms-session/      # Session management, SQLite persistence, episodic summary storage
                     #   sqlite/session_summaries.rs — per-session episodic summary persistence
  alms-sandbox/      # Builtin native tools (echo, math, http_get, shell, fs_*, etc.) + tool registry
  alms-channel/      # Channel adapters (Telegram polling implemented)
  alms-cli/          # CLI entrypoint (clap) — gateway, health, agent/session/run/job management
docs/                # Design docs — api.md, architecture.md, security-model.md, etc.
                     #   agent-runtime-design.md — detailed design for config/context/workspace
                     #   agent-ux-requirements.md — Alper's UX requirements
                     #   system-prompts.md — prompt file inventory and assembly order
                     #   _archive/ — archived/superseded docs
                     #   research/ — competitive analysis and tech-stack decisions
```

### Dependency graph (no cycles, 9 crates)

```
alms-cli → alms-gateway → alms-runtime      → alms-core
                        → alms-tools        → alms-core
                                            → alms-session
                                            → alms-sandbox
                        → alms-coordinator  → alms-core
                                            → alms-session
                                            → alms-runtime
                                            → alms-tools
                        → alms-channel      → alms-core
                        → alms-session      → alms-core
           alms-runtime → alms-sandbox      → alms-core
                        → alms-session
         → alms-session
```

## Code Conventions

- **Formatting**: `cargo fmt` — enforced in CI, zero tolerance
- **Linting**: `cargo clippy -- -D warnings` — all warnings are errors
- **Edition**: Rust 2024 (nightly)
- **Error handling**: `thiserror` for library errors, `anyhow` in CLI/binary code
- **Logging**: `tracing` with structured fields and `#[instrument]` macros
- **Concurrency**: `tokio` async runtime, `DashMap` for concurrent maps, `parking_lot` for locks
- **IDs**: Newtype wrappers (`AgentId`, `SessionId`, `RunId`, etc.) — never raw strings
- **Tests**: `#[cfg(test)] mod tests` in each file. Golden tests for SSE in `alms-gateway/tests/`

## Git Workflow

- Feature branches: `fix/<name>` or `feature/<name>`
- PRs target `main` — always use branches + PRs, never commit directly to main
- Run `make ci` before pushing

## Current State (as of 2026-05-06)

**Released**: v0.2.3 stable on `main` (tag `v0.2.3`, branch `release/0.2.3`). GitHub release with Linux x86_64 + Windows x86_64 binaries published. Headline v0.2.3 changes: workspace v2 single-sandbox-root model (#945/#946/#947/#948), Anthropic no-args tool fix (#967/#968), structured `MISSING_MODEL_AFTER_PROVIDER_SWITCH` 400 (#863/#960), per-tool structured output renderers (#873/#952/#970), per-run config override path removed (#941). v0.2.4 milestone in progress.

**Operator-facing default changes on the v0.2.4 track (PR #1081)**: (a) `LlmConfig::default().model` flipped from `moonshotai/kimi-k2.5` to `moonshotai/kimi-k2.6` on the `openrouter` provider — affects deployments with no explicit `[llm].model` in `alms.toml` or env override; (b) `AnthropicConfig::default().thinking_budget_tokens` flipped from `0` (disabled) to `2048` (enabled) — existing Anthropic deployments that did not explicitly set `[llm.anthropic].thinking_budget_tokens = 0` in `alms.toml` will start paying for ~2048 thinking tokens per turn after upgrading. The two-layer precedence chain from #767/#941 still applies: per-agent `Some(0)` (or `PATCH /agents/{id}` with `clear_thinking_budget_tokens: true`) overrides the new default; operators who want extended thinking off across the fleet pin `[llm.anthropic].thinking_budget_tokens = 0` in `alms.toml` or PATCH `/settings` with `llm.anthropic.thinking_budget_tokens = 0`.

**Operator-facing default change on the v0.2.4 track (PR #1160 / #987)**: agent-loop hard caps now default **ON** — `[llm].max_iterations` defaults to `500` and `[llm].max_run_duration_secs` defaults to `14400` (4 hours). They bound a single run's LLM-call count and wall-clock duration respectively, and apply to **all run types** (web chat, cron jobs, subagents — inherited verbatim by subagents), not just DMs. After upgrading, a deployment whose runs were previously unbounded will now end any run that exceeds 500 LLM calls or 4 hours as `failed` (for a peer-triggered DM run the gateway's completion gate converts the cap trip into an `Errored` conversation end so the peer is notified). Each cap is independently disabled by setting it to `0` (no limit) — the escape hatch for workloads that genuinely need unbounded runs (e.g. a >4 h scheduled job or a deep autonomous turn beyond 500 LLM calls). Both knobs are config-file-only: not mutable via `PATCH /settings`, no `ALMS_*` env override, restart required. See `docs/config.md` § "Agent-loop hard caps".

**Working**: core types, unified config, session management, agent runtime with tool loop + context builder + workspace integration, HTTP gateway with SSE, Telegram adapter, SQLite persistence (`./.alms/alms.db`), agent workspace files (personality/goals/memories/user under `<project>/.alms/agents/<name>/` post-#945 — auto-created on `alms agent create`), bootstrap interview, builtin tools (echo, math, http_get, shell [bash -c, persistent cwd, background execution, 30KB truncation; shell_exec alias preserved; config-file-only `[tools].shell_engine` knob (#1143) — `system-bash` default unchanged, opt-in `builtin` re-execs `current_exe()` as the hidden `alms shell-host` subcommand evaluating bash syntax via the embedded brush interpreter (command over stdin, file-scoped Landlock exec grant on the binary, exit 126 = host failure; see security-model.md § 4.2a)], fs_read, fs_write, fs_list, fs_edit, fs_grep, fs_glob, workspace_write, invoke_agent, read_subagent_session, send_message, list_agents, read_messages, ignore_message, list_my_sessions, read_session), FileStateCache (per-run read-before-write guard for fs_write/fs_edit — rejects writes to files the agent has not read; detects external modifications via mtime + content-hash fallback), approval workflow (guarded posture; #1109 — denying an approval is a stop, not a soft tool error: the denied run records a `{"user_denied": true}` tool result, goes to `cancelled` (emits `run_cancelled`), and still-`Queued` runs on the same session are cancelled too so the per-agent queue can't auto-advance into them — a queued run whose cancel token isn't registered yet is skipped and left `Queued`, structural fix in #1142), cron/scheduler, scheduled jobs (SQLite-backed), audit log, web UI with agent selector dropdown + session sidebar + dedicated Agents panel + workspace/jobs/audit panels + agent onboarding UI, multi-agent (hierarchy + peer messaging — foreground and background subagents via invoke_agent; peer-to-peer DM initiated via send_message tool + MessageBus + DM sessions with perspective mapping — DM replies are implicit since #1154: a peer-triggered DM run's final assistant text IS the reply, delivered by the gateway-side DM completion gate (`dm_lifecycle::handle_dm_run_completion` — every peer DM run exits as exactly one of delivered | ended | errored, never silence; one bounded `DM_EMPTY_REPLY_RETRY` nudge in the agent loop when the run is about to end with no deliverable text, then `DM_EMPTY_REPLY` + errored end with peer notification; `send_message` aimed at the current peer is folded — valid only for contacting a *different* agent mid-DM); DM sessions are agent-only: `POST /runs` on a `dm:` session returns structured `400 DM_SESSION_NOT_DIRECTLY_RUNNABLE` (#1156 Option C) because peer turns are triggered exclusively via MessageBus `RunTrigger`s (`is_peer_message: true`), and the implicit-reply machinery (DM recipient prompt + peer-fold) is armed only for peer-triggered runs as defense-in-depth; named subagents with registry-based config + workspace; persistent sessions via UUID v5; on-demand context via read_subagent_session; parallel tool execution via join_all; threshold-driven `compact` context strategy (#869, refined PR #1012 — renamed from `sliding-summary`; fires when assembled history crosses `compact_trigger_pct` of the effective history budget (`max_input_tokens` minus system / input / episodic / reserve overhead) and retains at most `compact_retain_pct` of the same effective budget worth of recent verbatim messages, default 0.80 / 0.40; legacy `recent_window` / `summary_interval` fields and `strategy = "sliding-summary"` accepted with one-time deprecation WARN at boot, rejected on the PATCH /settings wire with `400 CONTEXT_LEGACY_FIELD_DEPRECATED`; PR #1012 also fixed a partial-mutation hole on PATCH where a same-PATCH `compact_trigger_pct` / `compact_retain_pct` pair that failed the gap-floor check could leave one or both knobs committed to live config)), bearer auth (`ALMS_AUTH_TOKEN`), graceful shutdown (Ctrl+C / SIGTERM → drain in-flight runs → flush WAL), agent registry (named persistent agents with SQLite CRUD + auto-migration from sidecar `./.alms/agent_id`), agent HTTP API (`/agents` CRUD — list, create, get, update, delete, set-default; resolves by UUID or name slug), per-agent config overrides (model/posture merged into runs with two-layer precedence: per-agent > server default — per-run overrides were removed in #941; agents are the single per-tenant config surface), CLI agent management (`alms agent {list, create, show, delete, set-default, config}`), CLI session management (`alms session {list, show, delete}`), CLI run commands (`alms run {create, list, show}` via HTTP API), CLI job commands (`alms job {list, show}` via SQLite, `alms job {create, cancel}` via HTTP API), shell completions (`alms completions <shell>`), browser dashboard (`alms dashboard`), tool call persistence (tool calls and results persisted to session DB and reconstructed into structured LLM messages during context building — full tool execution history survives across runs, parallel tool calls grouped into single assistant message; DM sessions ARE included since #685/#988 — DM tool calls/results persist to session history as `Role::User` messages with `message_type: "reasoning"` metadata (preserving the DM all-User invariant) AND to the per-run `run_tool_calls` table, retrievable via `GET /runs/{run_id}/tool-calls`; partial tool call records persisted on error/cancellation for debugging), multi-provider LLM support (OpenAI/OpenRouter + Anthropic Messages API — provider selected via `llm.provider` config or `ALMS_LLM_PROVIDER` env var; Anthropic streaming, tool use, and response format fully mapped), cross-session episodic memory (run summaries generated after each run via heuristic or LLM mode, stored in `session_summaries` SQLite table, injected into context window for cross-session awareness; `list_my_sessions` and `read_session` tools for on-demand session recall; configurable via `run_summary_mode`/`run_summary_budget` with 15% hard cap), DM conversation lifecycle (`ignore_message` and depth-exceeded trigger `end_conversation` with `dm_ended` session markers, depth counter reset, `ConversationEnded` peer notification via dedicated `notifications:{agent}` sessions, `dm_conversation_ended` SSE event; `DEPTH_EXPIRY_SECS` raised to 1800s; see #384 Phases 1-7), workspace v2 — single project-root sandbox (#945 — every agent's `fs_*` and `shell` tools enforce against `<project_root>` as a single sandbox boundary set by `with_project_root`; agent metadata moved from `workspaces/<agent>/` to flat `.alms/agents/<name>/`; sibling reads work through the primary root by construction with no extras list), operator full-OS-access escape hatch (#947 — `[security].allow_full_os_access = ["agent-name"]` in `alms.toml` opts a named agent out of the project-root sandbox via `with_unrestricted_filesystem`; config-file-only and rejected by `PATCH /settings` with `400 SECURITY_KNOB_NOT_PATCHABLE`; boot-time + per-run WARN at `target = "alms.security"`; `shell_permissions` and the destructive-command classifier still apply because they are independent operator policy), per-agent git worktree mode (#946 — `alms agent create --worktree-mode git` provisions `<project>/.alms/worktrees/<name>/` on branch `alms/<name>` and pins the agent's sandbox at that worktree path; `<project>/.alms/agents/` is added as a read-only extras root via `with_extra_fs_read_root` so cross-agent metadata reads keep working from inside the worktree; non-git projects fail with `400 WORKTREE_REQUIRES_GIT`; `--force` discards uncommitted changes on remove; `[security].allow_full_os_access` precedes worktree mode at runtime, with a boot-time WARN on overlap), permission-based shell allow/deny list (#717 — `[tools.shell_permissions]` in `alms.toml` with regex `allowed_commands`/`denied_commands`; deny wins, empty allowlist = denylist-only mode; compiled once at startup into `CompiledPermissions` baked into each `ShellTool` and inherited by subagents; runs before the hardcoded destructive-command denylist and risk classifier as defense-in-depth; not mutable via `PATCH /settings`), non-bypassable shell classifier floor (#745 — in `Warn` and `Enforce` modes the destructive-command classifier blocks regardless of `allowed_commands`; operator-only `classifier_overrides` regex list in `[tools.shell_permissions]` is the sole escape hatch; `Off` remains the genuine opt-out; `classifier_overrides` is config-file-only and never PATCH-mutable), Anthropic extended thinking passthrough (#767 — `[llm.anthropic].thinking_budget_tokens` enables Claude 4.x extended thinking; streamed `thinking_delta` chunks are forwarded as provider-neutral `RuntimeEvent::ReasoningDelta` / `reasoning_delta` SSE events and rendered in a collapsible reasoning panel on the web UI; reasoning text is persisted as `reasoning_blocks` metadata on the assistant message so it survives page reload; two-layer precedence per-agent > `[llm.anthropic]` default (per-run overrides removed in #941); `Some(0)` at either layer explicitly disables; signatures and interleaved-thinking replay are out of scope — the `RuntimeEvent` shape is designed for #768 / #769 to layer on OpenAI/DeepSeek/xAI/Gemini reasoning using the same persistence and UI component), OpenAI-compat reasoning models (#768 — `[llm.openai].reasoning_effort` (`low`/`medium`/`high`/`minimal`) for OpenAI o-series / GPT-5 / xAI Grok reasoning variants; auto-stripped on DeepSeek R1 and non-reasoning models; response-side `reasoning_content` / `reasoning_summary` / `reasoning` coalesced with priority order into the provider-neutral `RuntimeEvent::ReasoningDelta` pipeline from #775; `TokenUsage.reasoning_tokens` plumbed end-to-end (adapter -> `RunOutput.usage` -> SSE `run_finished` + `GET /runs/{id}` + subagent completion markers, not yet persisted to the `runs` table); two-layer precedence per-agent > `[llm.openai]` default (per-run overrides removed in #941); CLI `alms agent create/config --reasoning-effort <value>`), Anthropic prompt caching (#766 — `[llm.anthropic].prompt_cache_enabled = true` default; adapter emits `cache_control: { type: "ephemeral" }` on the trailing system content block and the last tool definition so Anthropic caches the full stable prefix (tools + system + workspace + optional episodic summary) for 5 minutes; server-level only (no per-agent/per-run override); tools sorted by name in `to_definitions()` for deterministic cross-turn byte parity; `cache_creation_input_tokens` / `cache_read_input_tokens` plumbed adapter -> `Usage` -> `TokenUsage` -> `RunOutput.usage` -> SSE `run_finished` + subagent completion markers + runs-tab UI; two cache breakpoints (not four — workspace and episodic remain inside the system string and benefit from prefix caching via the single trailing-system marker); non-Anthropic providers and `prompt_cache_enabled = false` keep the pre-#766 wire shape byte-identical via `skip_serializing_if` and plain-string `system` field), Gemini context caching + thinking passthrough (#769 — `[llm.gemini]` adds `cache_enabled = true` (default), `cache_ttl_seconds = 300` (default), and `thinking_budget: Option<u32>` (None = disabled). Caching uses Gemini's `cachedContents` REST resource — an in-process `GeminiCacheStore` keyed by `SessionId` holds cache names across turns; a prefix-hash sentinel invalidates the handle when system instruction / tools drift, and cache-not-found errors from `generateContent` transparently invalidate and retry once without `cachedContent`. 32,768-token minimum is enforced server-side; prefix-too-small rejections are memoized as a `TooSmall` sentinel to suppress hammer-creation. `thinking_budget` emits `generationConfig.thinkingConfig: { thinkingBudget, includeThoughts: true }`; `thought: true` parts route into the provider-neutral `RuntimeEvent::ReasoningDelta` pipeline from #775, same UI panel as Anthropic / OpenAI. `usageMetadata.thoughtsTokenCount` threads through `Usage::reasoning_tokens` (flat shape); `usageMetadata.cachedContentTokenCount` threads through `Usage::cache_read_input_tokens` (shared surface with Anthropic — Gemini has no separate creation-side counter, `cache_creation_input_tokens` stays None). Thinking budget uses two-layer precedence per-agent > `[llm.gemini].thinking_budget` default (#794 / #941; per-run path removed); `Some(0)` at either layer explicitly disables; CLI `alms agent create/config --gemini-thinking-budget <N>` and `POST /agents` carry the override; named subagents inherit the parent's effective budget when their registry record leaves the field `None`. Caching is server-level only (no per-agent / per-run override) and inherited verbatim by subagents. Non-Gemini providers keep pre-#769 wire shape byte-identical via `skip_serializing_if`), `/settings` LLM surface + per-agent clear-sentinel (#809 — `PATCH /settings` and `GET /settings` gain a nested `llm` block covering all six reasoning / caching knobs across `[llm.anthropic]` / `[llm.openai]` / `[llm.gemini]`; mutations write through to the live `AgentConfig` so new runs pick up the value without a daemon restart; `PersistedSettings` grows a matching `llm` section so PATCH mutations survive restart; `openai.reasoning_effort = ""` clears back to "don't send on the wire". `UpdateAgentRequest` gains three additive `clear_*` booleans — `clear_thinking_budget_tokens`, `clear_reasoning_effort`, `clear_gemini_thinking_budget` — to reset per-agent reasoning overrides back to `None` (inherit server default) without requiring DELETE+recreate; sending both a value and `clear_*: true` for the same knob returns `400 CLEAR_AND_VALUE_CONFLICT`; Option A from the issue (strictly additive, non-breaking wire shape). Unblocks #804 Slices A + B), live foreground subagent session id on parent stream (#1105 — new `subagent_started` SSE event emitted by the coordinator's `spawn_subagent` the moment the subagent's session row is created, ahead of any nested `tool_start` from inside the subagent. Carries the parent's `tool_invocation_id` so the web UI's SubagentBar resolver can attach the new `subagent_session_id` to the right entry — including ephemeral / unnamed subagents where `subagent_name` alone cannot disambiguate concurrent invocations. Fires for both foreground and background paths; idempotent on the client because the `invoke_agent` tool result and the `subagent_completed` event still carry the same session id. The companion `subagent_completed` event already existed for the background path; pre-#1105 the foreground UI had to wait until `tool_end` (i.e. until the subagent finished) before the "View session" button became live. Threaded end-to-end via a new `ToolContext { invocation_id }` parameter on `Tool::execute_with_context` (default impl falls through to `Tool::execute(params)` so non-invoke_agent tools are unaffected), `SubagentDispatcher::dispatch{,_background}.parent_tool_invocation_id`, and `EventForwarder::forward_subagent_started` (default impl is a no-op). Frontend leg landed in #1113 (Iris). Ordering invariant — parent's `tool_start (invoke_agent)` precedes `subagent_started` precedes any nested `tool_start` — is enforced for the foreground path by the runtime channel's FIFO semantics: both events ride the same `RuntimeEventSender`, `tool_start` is queued before `tool.execute()` runs, `spawn_subagent` queues `subagent_started` next, and the subagent task's own events can only fire once its loop begins after that point. For the background path the same ordering is preserved by routing the bg-channel `SubagentStarted` back through the parent's `runtime_tx` via an Arc clone of `invoke_agent_fwd` held by the bg event forwarder task in `runs/lifecycle.rs` — `forward_runtime_events` then converts to SSE in strict FIFO order with the parent's `tool_start`, which the agent loop enqueues onto `runtime_tx` before `tool.execute()` runs (and therefore before the bg task can observe `SubagentStarted` on `bg_event_rx` at all)).

**Not yet real**: recursive subagent spawning (subagents can't yet spawn sub-subagents), progress reporting, invoke_agent result truncation (full responses still stored in parent context). See `docs/autonomous-subagents-design.md`.

See `docs/TASKS.md` for the prioritized task list, `docs/agent-runtime-design.md` for the runtime design, and `docs/agent-ux-requirements.md` for UX requirements.
